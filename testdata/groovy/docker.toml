name = "geode/testdata-groovy"

base = "ubuntu:groovy"
user = "jeff"

locale = "en_US.UTF-8"
timezone = "America/New_York"

command = ["/usr/bin/zsh", "-c", "echo '['$(date +%T)] $(hostname)"]
init = ["./init"]

[bind]
"/Users" = "/Users"
".ssh" = ".ssh"

[ports]
8000 = 8080
